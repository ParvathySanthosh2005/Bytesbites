<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>My Orders | ByteBites</title>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body style="background:#d27bb6;">

<div class="container py-5">
    <h2 class="text-center mb-4">My Orders</h2>

    {% if orders %}
        {% for order in orders %}
        <div class="card p-3 mb-3 shadow-sm">
            <h5 class="mb-1">{{ order['name'] }}</h5>

            <p class="mb-1">
                Quantity:
                <strong>{{ order['quantity'] }}</strong>
            </p>

            {% if order['status'] == 'Pending' %}
                <span class="badge bg-warning text-dark">Pending</span>
            {% elif order['status'] == 'Preparing' %}
                <span class="badge bg-info">Preparing</span>
            {% elif order['status'] == 'Completed' %}
                <span class="badge bg-success">Completed</span>
            {% else %}
                <span class="badge bg-secondary">{{ order['status'] }}</span>
            {% endif %}
        </div>
        {% endfor %}
    {% else %}
        <div class="card p-4 text-center shadow-sm">
            <p class="mb-0">You haven’t placed any orders yet 🍽️</p>
        </div>
    {% endif %}

    <div class="text-center mt-4">
        <a href="/customer_dashboard" class="btn btn-dark">
            ⬅ Back to Dashboard
        </a>
    </div>
</div>

</body>
</html>
